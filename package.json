{
  "name": "test-deploy",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run setup:client && npm run setup:server",
    "setup:client": "cd test-deploy.client && npm i",
    "setup:server": "cd test-deploy.server && npm i",
    "start": "node test-deploy.server/index.js"
  },
  "engines": {
    "node": "^14.15.4"
  }
}